import streamlit as st

st.set_page_config(page_title="Fill the blanks with options")

st.markdown("""
    <style>
    h1, h2, h3, h4, h5, h6, p {
        color: #000000;
    }
    </style>
""", unsafe_allow_html=True)


st.markdown("# Fill the blanks with options")

st.write(
    """Use the word given in capitals at the end of some of the lines to form a word that fits in the gap **in the same line**."""
)

